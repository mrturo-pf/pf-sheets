{
  "name": "pf-sheets",
  "version": "0.1.0",
  "private": true,
  "description": "Google Apps Script (bound to a Google Sheet) source, versioned in Git and deployed via clasp. Syncs pf-rates exchange rates into a spreadsheet.",
  "scripts": {
    "lint": "eslint src tests",
    "test": "jest",
    "test:coverage": "jest --coverage",
    "check": "npm run lint && npm run test:coverage"
  },
  "devDependencies": {
    "@google/clasp": "3.4.1",
    "eslint": "^9.9.0",
    "jest": "^29.7.0"
  },
  "engines": {
    "node": ">=18"
  }
}
